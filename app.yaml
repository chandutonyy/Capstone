env: flex
runtime: python39  # replace with your Python version
instance_class: F2
automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 15
entrypoint: gunicorn -b :$PORT appflask:app

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
